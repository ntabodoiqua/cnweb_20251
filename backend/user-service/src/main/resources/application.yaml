server:
  port: 8081
spring:
  application:
    name: user-service
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://db-postgresql-sgp1-29269-do-user-23301452-0.k.db.ondigitalocean.com:25061/user_service?sslmode=require}
    username: ${SPRING_DATASOURCE_USERNAME:doadmin}
    password: ${SPRING_DATASOURCE_PASSWORD:changeme}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  cache:
    type: redis
  rabbitmq:
    addresses: ${SPRING_RABBITMQ_ADDRESSES:amqps://xqjmojji:changeme@fuji.lmq.cloudamqp.com/xqjmojji}
    username: ${SPRING_RABBITMQ_USERNAME:xqjmojji}
    password: ${SPRING_RABBITMQ_PASSWORD:changeme}
    virtual-host: xqjmojji
    ssl:
      enabled: true
  data:
    redis:
      host: db-valkey-sgp1-03029-do-user-27848320-0.m.db.ondigitalocean.com
      port: 25061
      username: default
      password: ${SPRING_DATA_REDIS_PASSWORD:changeme}
      ssl:
        enabled: true
eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_CLIENT_SERVICEURL_DEFAULTZONE:http://discovery-service:8761/eureka/}
  instance:
    prefer-ip-address: true
jwt:
  signerKey: cnf61zcey4yE0RfDwk3yOAgi+CRVsjb1LPQP0O9e0GyLworFq8IthDFERD/0bxy2
  valid-duration: 3600
  refreshable-duration: 86400

internal:
  service:
    secret: ${INTERNAL_SERVICE_SECRET:InT3rn@l-S3rv1c3-S3cr3t-K3y-2025-VDT}

google:
  client:
    id: ${GOOGLE_CLIENT_ID:872174769216-ib5us0n2aom07l8hq3np9pqje3r81rp2.apps.googleusercontent.com}
    secret: ${GOOGLE_CLIENT_SECRET:changeme}

file:
  directory: ./uploads/
  max-size: 10MB

# Management endpoints for monitoring
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
      base-path: /actuator
  endpoint:
    health:
      show-details: always
    prometheus:
      enabled: true
  metrics:
    tags:
      application: ${spring.application.name}
  prometheus:
    metrics:
      export:
        enabled: true

# Logging configuration for Loki
logging:
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %logger{36} - %msg%n"
  level:
    root: INFO
    org.springframework.security: DEBUG
