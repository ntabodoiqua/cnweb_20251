import { ProfileHistory, mockTransactions } from "../../components/profile";

const ProfileHistoryPage = () => {
  return <ProfileHistory transactions={mockTransactions} />;
};

export default ProfileHistoryPage;
