import { ProfileAddresses, mockAddresses } from "../../components/profile";

const ProfileAddressesPage = () => {
  return <ProfileAddresses addresses={mockAddresses} />;
};

export default ProfileAddressesPage;
