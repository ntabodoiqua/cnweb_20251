# Development: Direct call to backend (CORS must be enabled on backend)
# Or use empty string to use full paths with /api prefix
VITE_BACKEND_URL=https://nguyentheanh-nta.id.vn

# WebSocket URL for notifications (phải dùng wss:// vì backend chạy HTTPS)
VITE_WS_URL=wss://nguyentheanh-nta.id.vn

# Google OAuth Client ID
VITE_GOOGLE_CLIENT_ID=822664978401-0d2al7gb5j17as935f17r2lu6hg6eafl.apps.googleusercontent.com
